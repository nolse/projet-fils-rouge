---
# Valeurs par défaut — surchargeables depuis le playbook ou l'inventaire
# Image Jenkins custom basée sur CentOS 7 (Jenkins + Nginx intégrés)
jenkins_image: sadofrazer/jenkins
# Port d'accès à l'interface web Jenkins
jenkins_port: 8080
# Port SSH vers le conteneur Jenkins
jenkins_ssh_port: 22000
# Port de communication avec les agents Jenkins
jenkins_agent_port: 50000
